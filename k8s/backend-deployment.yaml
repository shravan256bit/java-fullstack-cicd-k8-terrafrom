apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: shravan256bit/auth-backend:v6
        ports:
        - containerPort: 8080
        envFrom:
        - secretRef:
            name: db-secret